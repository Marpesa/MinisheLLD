

-debug

-norm

$USERtruc doit expnd USER et ecrire truc

-isatty pour subminishell (exit | ./minishell)



valgrind --suppressions=supp_file --leak-check=full --show-leak-kinds=all ./minishell


bien gerer le ctrl \ quand cat wait (il doit quitter)

0 : succès
1 : erreur générique
2 : erreur de syntaxe dans la ligne de commande
126 : la commande appelée ne peut être exécutée
127 : la commande appelée n'a pas été trouvée
128 : erreur de script (par exemple, une erreur de variable d'environnement)
130 : le script a été interrompu par l'utilisateur (par exemple, via Ctrl+C)
255 : statut de sortie maximal (la valeur maximale autorisée est 255)
            
static void	nested_quotes(t_quotes *quotes, int i, char *str)
{
	if (str[i] == '\'' && quotes->in_s == true && quotes->in_d == true)
		quotes->s_in_d = true;
}